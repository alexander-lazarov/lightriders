package graphics

import "github.com/alexander-lazarov/lightriders/models"

const tileWidth = 12
const tileHeight = 12
const tileGap = 2

const TotalWidth = tileWidth * models.BoardWidth
const TotalHeight = tileHeight * models.BoardHeight
